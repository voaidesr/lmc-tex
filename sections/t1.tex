% New lecture/section content starts here.
\chapter{ Introducere în LP și Prolog}

\section{Administrativ}

\begin{itemize}
	\item Cursurile d-lui Leuștean sunt foarte bine scrise și chiar merită citite pentru a învăța la materia asta. Le găsiți pe toate pe
	      GitHub. În plus, la examen \textbf{o să fie nevoie} să folosiți propoziții din curs (care sunt numerotate), și să le citați, așa că vă ajută să știți cam care este structura cursului. Nu o să reluăm teoria pe care o prezintă în curs \textbf{decât} dacă ne cereți asta.

	\item Seminariile sunt destul de complexe, acoperă ok materia, iar soluțiile sunt postate pe site, așa că noi o să încercăm să lucrăm la tutoriat exerciții extra.

	\item Puteți să ne scrieți oricând în privat/pe
	      grup cu întrebări/sugestii/idei, iar noi o să încercăm să vă ajutăm și să le punem în practică. De asemenea, puteți să ne sugerați ce ați
	      refera să lucrăm la tutoriat.

	\item O să publicăm pe GitHub tutoriatele săptămânal și o să anunțăm, prin README, ce o să facem la următorul tutoriat. De asemenea, plănuim să creăm liste de exerciții, la care să publicăm și rezolvările. Dacă o să doriți să ne uităm pe rezolvările voaste, puteți să ne ziceți.
\end{itemize}

\section{De ce Logică?}

Este foarte normal să vă întrebați de ce e nevoie să studiați logica ca studenți la info, având în vedere că e aproape matematică
pură, care nu pare să aibă foarte mare aplicabilitate în viața reală.


\noindent Well, sunt mai multe motive.

\subsection*{Logica e baza informaticii}

Aproape toți oamenii care au fondat informatica erau logicieni. Tot ce înseamnă informatică formală se poate traduce în logică, \href{https://en.wikipedia.org/wiki/Curry%E2%80%93Howard_correspondence}{literally.}


\newpage

Asta se traduce în două lucruri:

\begin{itemize}
	\item nu o să scăpați de logică pentru încă ceva timp, în anul II aveți două materii care se construiesc pe logică și la care o să vă ajute foarte mult să aveți bazele bine stabilite.

	\item felul foarte formal de a gândi care este promovat de logică vă va ajuta foarte mult în a deveni programatori mai buni; o să ajungeți să gândiți mult mai mult ce scrieți, atât în matematică cât și în programare, pentru a fi mult mai riguros și
	      mai logic exprimat, ceea ce o să vă ajute mai ales atunci când aveți lucruri mai complicate de făcut.
\end{itemize}

Logica vă deschide drumul și către o ramură destul de nișată a informaticii, dar care este foarte bine plătită și foarte AI-proof, anume \href{https://en.wikipedia.org/wiki/Formal_verification}{verificarea formală}, care,
pe scurt, presupune demonstrarea matematică a corectitudinii unui cod, lucru care este esențial în sisteme high-risk (procesoare, sisteme medicale, rachete etc.) Facultatea noastră este foarte bogată în logicieni, se țin
foarte des seminarii de logică la \href{https://ilds.ro/}{ILDS} sau \href{https://los.cs.unibuc.ro/}{LOS}, așa că dacă vreți să faceți cercetare în timpul facultății sau să vă specializați pe partea de
verificare formală sau alte tipuri de logică, chiar aveți cu cine.

\section{Logica Propozițională}

Formulele stau la baza Logicii Propoziționale și sunt principalul lucru cu care o să lucrați capitolul ăsta. Cursul menționează că ele sunt definite inductiv:

\begin{definition}[Formule]

	Formulele sunt definite astfel:

	\begin{description}
		\item[(1)] Orice variabilă propozițională este formulă. (Adică $\forall v \in V$, $v$ este formulă)
		\item[(2)] Dacă $\varphi$ este formulă, atunci $\neg \varphi$ este formulă
		\item[(3)] Dacă $\varphi, \psi$ sunt formule, atunci $\psi \to \varphi$ este formulă
	\end{description}

\end{definition}

Dar de ce este inductiv? Până acum, ați folosit doar inducția pe numere naturale, care știți cum funcționa. Aveam un caz de bază \( P(0) \) și, presupunând că
predicatul era adevărat pentru un \( n \) natural, construiam următorul caz, \( n + 1 \), și demonstram corectitudinea.

Practic, inducția reprezintă metoda de a pleca de la ceva particular și de a generaliza incremental. Pentru numere naturale, pasul incremental este simplu, anume adunarea.

Noi putem extinde metoda asta, astfel, și la formule. Avem cazul particular, pe care îl definim a fi formulă, adică variabilele \( v \). De aici, incremental, construim
toate celelalte formule. În loc de un singur pas incremental, avem doi:

\begin{description}
	\item[\( \neg \)] putem nega formula noastră curentă pentru a obține o nouă formulă
	\item[\( \to \)] atunci când avem două formule, o nouă formulă este definită prin implicație
\end{description}

Această metodă se numește \textbf{inducție structurală}.

\noindent În următoarea figură puteți vedea cum sunt construite formulele din doar două variabile.

\begin{center}
	\begin{tikzpicture}[
			% Global styles - Scaled down
			scale=0.8, transform shape,
			every node/.style={align=center, font=\sffamily},
			setNode/.style={draw=black!60, thick, rounded corners, fill=blue!5, inner sep=8pt, minimum width=3cm},
			ruleEdge/.style={->, >=Stealth, thick, draw=gray!80},
			ruleLabel/.style={fill=white, inner sep=2pt, font=\scriptsize\color{red!70!black}},
			subsetEdge/.style={->, >=Stealth, thick, dashed, draw=gray!60}
		]

		% ----------------- LEVEL 0 -----------------
		\node[setNode] (L0) {
			\textbf{Nivel 0} ($\Phi_0$)\\
			$\{v_0, v_1\}$
		};

		% ----------------- LEVEL 1 (Branching Rules) -----------------
		\node[setNode, below left=1cm and -0.8cm of L0, fill=green!5] (L1_neg) {
			\textbf{Negare}\\
			$\{\neg v_0, \neg v_1\}$
		};

		\node[setNode, below right=1cm and -0.8cm of L0, fill=green!5] (L1_imp) {
			\textbf{Implicație}\\
			$\{v_0 \to v_1, \dots\}$
		};

		% ----------------- LEVEL 1 (Union) -----------------
		\node[setNode, below=3.2cm of L0] (L1_union) {
			\textbf{Nivel 1} ($\Phi_1$)\\
			$\Phi_0 \cup \{\neg v_0, v_0 \to v_1, \dots\}$
		};

		% ----------------- LEVEL 2 -----------------
		\node[setNode, below=1.2cm of L1_union] (L2) {
			\textbf{Nivel 2} ($\Phi_2$)\\
			$\Phi_1 \cup \{\neg(\neg v_0), \neg v_0 \to v_1, \dots\}$
		};

		% ----------------- INFINITY -----------------
		\node[below=0.6cm of L2] (dots) {\huge $\vdots$};

		\node[setNode, below=0.5cm of dots, fill=yellow!10] (L_inf) {
			\textbf{Nivel} $\infty$ ($Form$)\\
			$\bigcup_{n=0}^{\infty} \Phi_n$
		};

		% ----------------- EDGES & ARROWS -----------------
		\draw[ruleEdge] (L0) -- (L1_neg) node[midway, ruleLabel, xshift=-8pt] {R(2)};
		\draw[ruleEdge] (L0) -- (L1_imp) node[midway, ruleLabel, xshift=8pt] {R(3)};

		\draw[ruleEdge] (L1_neg) -- (L1_union);
		\draw[ruleEdge] (L1_imp) -- (L1_union);
		\draw[subsetEdge] (L0) -- (L1_union) node[midway, fill=white, inner sep=1pt, font=\scriptsize] {$\subseteq$};

		\draw[ruleEdge] (L1_union) -- (L2) node[midway, ruleLabel] {Aplicăm R(2) \& R(3)};

		\draw[ruleEdge] (L2) -- (dots);
		\draw[subsetEdge] (dots) -- (L_inf);

	\end{tikzpicture}
\end{center}

\subsubsection{Principiul inducției pe formule}

Același principiu poate fi a aplicat pentru a demonstra că orice formulă respectă o proprietate \textbf{P} (e.g., orice formulă are mai \( \leq \) conectori logici
decât variabile, orice formulă este bine parantezată, etc.).

\begin{prop}

	Fie \textbf{P} o proprietate. Presupunem că:

	\begin{description}
		\item[(0)] Orice variabilă \( v \) are proprietatea \textbf{P}.
		\item[(1)] Pentru orice formulă \( \varphi \), dacă \( \varphi \) are proprietatea \textbf{P}, atunci \( \neg \varphi \) are proprietatea \textbf{P}.
		\item[(2)] Pentru orice formule \( \varphi, \psi \), dacă acestea au proprietatea \textbf{P}, atunci \( \varphi \to \psi \) are proprietatea \textbf{P}.
	\end{description}

	\noindent	Atunci orice formulă \( \varphi \) are proprietatea \textbf{P}.

\end{prop}

\begin{eg}

	Vrem să demonstrăm că orice formulă \( \varphi \) este parantezată corect. Formal: orice formulă \( \varphi \) are proprietatea \( \mathbf{P}(\varphi) : st(\varphi) = dr(\varphi) \),
	unde \( st(\varphi) \) este numărul de paranteze "(", iar \( dr(\varphi) \) este numărul de paranteze ")".

\end{eg}

\begin{proof}
	Demonstrăm proprietatea $P(\varphi)$ prin inducție structurală după construcția formulei $\varphi$.

	\begin{description}
		\item[Cazul de bază:] $\varphi = v$, unde $v \in V$ este o variabilă propozițională.
			Conform definiției sintaxei, variabilele nu conțin paranteze.
			\[ st(v) = 0 \text{ și } dr(v) = 0 \implies st(v) = dr(v) \]
			Deci $P(v)$ este adevărată.

		\item[Pasul inductiv:] Presupunem că proprietatea este adevărată pentru formulele $\psi$ și $\chi$ (Ipoteza Inductivă). Deci $st(\psi) = dr(\psi)$ și $st(\chi) = dr(\chi)$.

			Demonstrăm pentru regulile de construcție:

			\begin{itemize}
				\item \textbf{Cazul} $\varphi = (\neg \psi)$:
				      Calculăm numărul de paranteze folosind definiția recursivă:
				      \begin{align*}
					      st(\varphi) & = st(\psi) + 1 \\
					      dr(\varphi) & = dr(\psi) + 1
				      \end{align*}
				      Din Ipoteza Inductivă știm că $st(\psi) = dr(\psi)$. Adunând $1$ în ambele părți, obținem $st(\varphi) = dr(\varphi)$.

				\item \textbf{Cazul} $\varphi = (\psi \to \chi)$:
				      Calculăm numărul de paranteze:
				      \begin{align*}
					      st(\varphi) & = 1 + st(\psi) + st(\chi) \\
					      dr(\varphi) & = dr(\psi) + dr(\chi) + 1
				      \end{align*}
				      Înlocuind din Ipoteza Inductivă ($st \to dr$), avem:
				      \[ st(\varphi) = 1 + dr(\psi) + dr(\chi) \]
				      Datorită comutativității adunării, $1 + dr(\psi) + dr(\chi) = dr(\psi) + dr(\chi) + 1$, deci $st(\varphi) = dr(\varphi)$.
			\end{itemize}
	\end{description}

	Conform principiului inducției structurale, proprietatea $P(\varphi)$ este adevărată pentru orice formulă $\varphi$.
\end{proof}



\section*{Prolog Example: Family Tree}

Below is an example of a simple Prolog program rendered directly in \LaTeX.

\begin{lstlisting}[caption={Family relations in Prolog}]
% This is a comment: defining facts
parent(albert, bob).
parent(albert, betsy).
parent(albert, bill).

parent(alice, bob).
parent(alice, betsy).
parent(alice, bill).

% Defining a rule: X is a sibling of Y
sibling(X, Y) :-
    parent(Z, X),
    parent(Z, Y),
    X \= Y. % Ensure a person isn't their own sibling
\end{lstlisting}
